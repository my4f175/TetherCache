"""Top-level convenience namespace.

Many helpers in :mod:`utils` (and the bundled :mod:`pipeline` /
:mod:`wan`) need to be importable from the repository root, matching the
layout assumed by the inference scripts.
"""
