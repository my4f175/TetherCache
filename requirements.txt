# Inference-only requirements for TetherCache + Self-Forcing.
# Training-only / GUI-only deps from the upstream Self-Forcing
# requirements.txt have been dropped (wandb, tensorrt, flask, lmdb, ...).

# --- Core --------------------------------------------------------------
torch>=2.4.0
torchvision>=0.19.0
einops
omegaconf
tqdm
imageio
imageio-ffmpeg
av==13.1.0
numpy==1.24.4

# --- Wan2.1 / model graph --------------------------------------------
diffusers==0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1
sentencepiece
ftfy
easydict

# --- Image I/O -------------------------------------------------------
opencv-python>=4.9.0.80
Pillow

# --- Hugging Face checkpoint download --------------------------------
huggingface_hub[cli]

# --- Required by Wan utils.fm_solvers --------------------------------
scikit-image

# --- Flash attention (compile separately with --no-build-isolation) --
# pip install flash-attn --no-build-isolation
